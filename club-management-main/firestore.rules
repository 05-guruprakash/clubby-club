rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // TEMPORARY: Allow everything for development to fix the blocker
    match /{document=**} {
      allow read, write: if true;
    }

  }
}
