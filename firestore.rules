rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Explicitly allow read/write for community_messages and everything else
    match /{document=**} {
      allow read, write: if true;
    }
  }
}