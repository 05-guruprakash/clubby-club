{
  "name": "backend",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/server.js",
    "frontend": "cd frontend && npm run dev",
    "build": "npm install && cd frontend && npm install && npm run build",
    "dev": "node -e \"const { spawn } = require('child_process'); spawn('npm', ['start'], { stdio: 'inherit', shell: true }); spawn('npm', ['run', 'frontend'], { stdio: 'inherit', shell: true });\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.0",
    "express-rate-limit": "^8.2.1",
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.2",
    "helmet": "^8.1.0"
  }
}